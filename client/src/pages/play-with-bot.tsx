import Layout from "../components/layout/layout";

const Page = () => {
    return (
        <div className="page">
            
        </div>
    );
};

const PlayWithBot = () => {
    return <Layout>{Page()}</Layout>
}

export default PlayWithBot;